/** *	@service HexEncode *	@desc Encodes data to Hex * *	@param data string [memory] optional default false * *	@return result string [memory] *	 *	@author Vibhaj Rajan <vibhaj8@gmail.com> ***/VaultGuard.core.service.HexEncode = {	input : function(){		return {			optional : { data : '' }		}	},		run : function($memory){		var $str = $memory['data'];				var $hex = '', x;		for (var i = 0; i < $str.length; i++){			x = $str.charCodeAt(i).toString(16);			if( x.length == 0 ) x = "00";			if( x.length == 1 ) x = "0" + x;			$hex += x;		}				$memory['result'] = $hex;		$memory['valid'] = true;		return $memory;	},		output : function(){		return ['result'];	}};