/** *	@service HexDecode *	@desc Decodes data from hex * *	@param data string [memory] optional default false * *	@return result string [memory] *	 *	@author Vibhaj Rajan <vibhaj8@gmail.com> ***/VaultGuard.core.service.HexDecode = {	input : function(){		return {			optional : { data : '' }		}	},		run : function($memory){		var $hex = $memory['data'];		var $str = '';				for (var i = 0; i < $hex.length; i += 2)			$str += String.fromCharCode(parseInt($hex.substr(i, 2), 16));					$memory['result'] = $str;		$memory['valid'] = true;		return $memory;	},		output : function(){		return ['result'];	}};